# sqlx's offline query cache is regenerated by `cargo sqlx prepare`, which
# always writes LF. Pin it to LF in the repo so a Windows checkout
# (core.autocrlf=true) does not report the whole directory as modified after
# every prepare. See docs/backend-alignment/STATUS.md.
.sqlx/*.json text eol=lf
migrations/*.sql text eol=lf
